// Config by ScriptedSnark for Scriptless category. 
// Edited by _Smiley

// Settings
bxt_hud_viewangles 1
bxt_hud_jumpspeed 1
gl_texturemode GL_NEAREST_MIPMAP_NEAREST
fps_max 100
clockwindow 0
skill 1
room_off 1
cl_bob 0
hud_fastswitch 1

// Bhop and Duckroll binds
bind mwheelup "+jump"
bind mwheeldown "+duck"

// force_centerview (UPDATE)
bind mouse5 "force_centerview"

// Binds for TC-Skip
bind alt "+strafe"
bind shift "+speed"

// Save and Load binds
bind c "save quick"
bind v "load quick"

// Load autosave? No problem
bind h "load autosave"

// Pause Buffering Binds
bind k "pause"
bind l "setpause"

// Pausable
bind j "pausable 0"

// For Jumpbug
bind b "-duck"

// FPS Binds
bind g "fps_max 23"
bind z "fps_max 20"
bind x "fps_max 100"
bind tab "fps_max 8"
bind mouse4 "fps_max 4"

// Zoom
bind KEY "default_fov 50"
bind KEY "default_fov 110"

// Weapon bind
bind capslock "weapon_hornetgun"
bind KEY "weapon_crowbar"
bind KEY "weapon_9mmhandgun"
bind KEY "weapon_357"
bind KEY "weapon_9mmAR"
bind KEY "weapon_shotgun"
bind KEY "weapon_crossbow"
bind KEY "weapon_rpg"
bind KEY "weapon_gauss"
bind KEY "weapon_egon"
bind KEY "weapon_handgrenade"
bind KEY "weapon_satchel"
bind KEY "weapon_tripmine"
bind KEY "weapon_snark"

// Start Run
bind p "bxt_timer_reset;stop;load xhair;w 50;map c1a0;bxt_timer_start;bxt_autorecord r"

// Hazard Course Start
bind KEY "bxt_timer_reset;stop;map t0a0;bxt_timer_start;bxt_autorecord r"

//Maxam FPS values for jumpbugs
bind KEY "fps_max 22.22222" // Xen
bind KEY "fps_max 29.8" // TCS
bind KEY "fps_max 23.25581" // OAR end
bind KEY "fps_max 20.40816" // Blast Pit
