// Config by ScriptedSnark for Scriptless category.

// V2 Version:
// - Added bind for "force_centerview"

// Unused (remove // in 4-5 line, if you want)
// bind i "default_fov 50"
// bind o "default_fov 110"

// Unused Fun (remove // in 8-12 line, if you want (Alpha 0.52 Reference))
// bind 6 "speak hgrunt/my.ass.is.heavy"
// bind 7 "speak scientist/greetings"
// bind 8 "speak scientist/youinsane"
// bind 9 "speak scientist/iwounded"
// bind 0 "speak barney/c1a4_ba_octo4"

// Settings
gl_texturemode GL_NEAREST_MIPMAP_NEAREST
fps_max 100
clockwindow 0
skill 1
room_off 1
cl_bob 0
hud_fastswitch 1

// Weapon bind
bind capslock "weapon_hornetgun"

// Bhop and Duckroll binds
bind mwheelup "+jump"
bind mwheeldown "+duck"

// force_centerview (UPDATE)
bind mouse5 "force_centerview"

// Binds for TC-Skip
bind alt "+strafe"
bind shift "+speed"

// Save and Load binds
bind c "save quick"
bind v "load quick"

// Load autosave? No problem
bind h "load autosave"

// Pause Buffering Binds
bind k "pause"
bind l "setpause"

// Pausable
bind j "pausable 0"

// For Jumpbug
bind b "-duck"

// FPS Binds
bind g "fps_max 23"
bind z "fps_max 20"
bind x "fps_max 100"
bind tab "fps_max 8"
bind mouse4 "fps_max 4"

// Start Run
bind p "bxt_timer_reset;stop;load xhair;w 50;map c1a0;bxt_timer_start;bxt_autorecord r"

// ScriptedSnark
echo Config by ScriptedSnark for Scriptless category activated!

speak scientist/sci_pain3